[build]
builder = "RAILPACK"
buildCommand = "composer install --no-dev --optimize-autoloader && php artisan storage:link --force || true"

[deploy]
startCommand = "php artisan config:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
