@extends('layouts.app')
@section('title', 'Dashboard — Kominhoo Beauty')

@section('content')
<section class="section" style="background:var(--cream)">
  <div class="container">

    @if(request()->query('order_placed'))
    <div style="background:#dcfce7;color:#166534;border:1px solid #86efac;border-radius:var(--r-lg);padding:16px 20px;margin-bottom:24px;display:flex;align-items:center;gap:12px">
      <span style="font-size:1.4rem">🎉</span>
      <div>
        <div style="font-weight:700">Order placed successfully!</div>
        <div style="font-size:.87rem;margin-top:2px">We've received your order and will begin processing it shortly.</div>
      </div>
    </div>
    @endif

    @if(session('success'))
    <div style="background:#dcfce7;color:#166534;border:1px solid #86efac;border-radius:var(--r-lg);padding:16px 20px;margin-bottom:24px">
      {{ session('success') }}
    </div>
    @endif

    @if(session('error'))
    <div style="background:#fee2e2;color:#991b1b;border:1px solid #fca5a5;border-radius:var(--r-lg);padding:16px 20px;margin-bottom:24px">
      {{ session('error') }}
    </div>
    @endif

    <div style="display:grid;grid-template-columns:260px 1fr;gap:40px;align-items:start">

      <!-- Sidebar -->
      <div style="background:#fff;border-radius:var(--r-xl);padding:28px">
        <div style="text-align:center;margin-bottom:24px">
          <div style="width:72px;height:72px;border-radius:50%;background:var(--lime);display:flex;align-items:center;justify-content:center;font-size:1.8rem;font-weight:800;margin:0 auto 12px">
            {{ strtoupper(substr($user['name'] ?? 'U', 0, 1)) }}
          </div>
          <div style="font-weight:800;font-size:1.05rem">{{ $user['name'] ?? 'Guest' }}</div>
          <div style="font-size:.82rem;color:var(--text-muted)">{{ $user['email'] ?? '' }}</div>
          <div style="margin-top:8px"><span class="badge badge-outline">{{ $user['tier'] ?? 'Bronze' }}</span></div>
        </div>
        <div style="border-top:1px solid var(--gray-200);padding-top:20px">
          <a href="{{ route('dashboard.index') }}" style="display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:var(--r-md);font-weight:600;margin-bottom:4px;{{ request()->routeIs('dashboard.index') ? 'background:var(--cream)' : '' }}">
            📊 Overview
          </a>
          <a href="{{ route('dashboard.orders') }}" style="display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:var(--r-md);font-weight:600;margin-bottom:4px;{{ request()->routeIs('dashboard.orders') ? 'background:var(--cream)' : '' }}">
            📦 My Orders
          </a>
          <a href="{{ route('quiz') }}" style="display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:var(--r-md);font-weight:600;margin-bottom:4px">
            🔬 Skin Quiz
          </a>
        </div>
        <div style="border-top:1px solid var(--gray-200);padding-top:16px;margin-top:8px">
          <form method="POST" action="{{ route('logout') }}">
            @csrf
            <button type="submit" style="width:100%;text-align:left;display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:var(--r-md);font-weight:600;color:var(--red);background:none;border:none;cursor:pointer">
              🚪 Sign Out
            </button>
          </form>
        </div>
      </div>

      <!-- Main Content -->
      <div>
        <div style="background:#fff;border-radius:var(--r-xl);padding:32px;margin-bottom:24px">
          <div style="display:flex;gap:24px;flex-wrap:wrap">
            <div style="flex:1;min-width:140px;background:var(--cream);border-radius:var(--r-lg);padding:20px;text-align:center">
              <div style="font-size:1.8rem;font-weight:800">{{ $user['loyalty_points'] ?? 0 }}</div>
              <div style="font-size:.78rem;color:var(--text-muted);font-weight:600;text-transform:uppercase;letter-spacing:.08em">Loyalty Points</div>
            </div>
            <div style="flex:1;min-width:140px;background:var(--cream);border-radius:var(--r-lg);padding:20px;text-align:center">
              <div style="font-size:1.8rem;font-weight:800">{{ count($orders) }}</div>
              <div style="font-size:.78rem;color:var(--text-muted);font-weight:600;text-transform:uppercase;letter-spacing:.08em">Orders</div>
            </div>
            <div style="flex:1;min-width:140px;background:var(--lime);border-radius:var(--r-lg);padding:20px;text-align:center">
              <div style="font-size:1.8rem;font-weight:800">{{ $user['tier'] ?? 'Bronze' }}</div>
              <div style="font-size:.78rem;font-weight:600;text-transform:uppercase;letter-spacing:.08em">Tier</div>
            </div>
          </div>
        </div>

        <!-- Recent Orders -->
        <div style="background:#fff;border-radius:var(--r-xl);padding:32px;margin-bottom:24px">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:20px">
            <h2 style="font-size:1.1rem;font-weight:800">Recent Orders</h2>
            <a href="{{ route('dashboard.orders') }}" class="btn btn-outline btn-sm">View All</a>
          </div>
          @if(count($orders))
            <div style="display:grid;gap:12px">
              @foreach($orders as $order)
                <div style="display:flex;justify-content:space-between;align-items:center;padding:16px;background:var(--cream);border-radius:var(--r-lg)">
                  <div>
                    <div style="font-weight:700">{{ $order['order_number'] }}</div>
                    <div style="font-size:.82rem;color:var(--text-muted)">{{ \Carbon\Carbon::parse($order['created_at'])->format('d M Y') }}</div>
                  </div>
                  <div style="text-align:right">
                    <div style="font-weight:700">&#8358;{{ number_format($order['total']) }}</div>
                    <span class="badge" style="background:var(--lime-tint)">{{ ucfirst($order['status']) }}</span>
                  </div>
                </div>
              @endforeach
            </div>
          @else
            <div style="text-align:center;padding:40px;color:var(--text-muted)">
              <div style="font-size:2.5rem;margin-bottom:12px">📦</div>
              <p>No orders yet. <a href="{{ route('shop') }}">Start shopping</a></p>
            </div>
          @endif
        </div>

        <!-- Update Profile -->
        <div style="background:#fff;border-radius:var(--r-xl);padding:32px">
          <h2 style="font-size:1.1rem;font-weight:800;margin-bottom:20px">Update Profile</h2>
          <form method="POST" action="{{ route('dashboard.profile.update') }}">
            @csrf
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
              <div>
                <label style="font-size:.82rem;font-weight:600;display:block;margin-bottom:6px">Full Name</label>
                <input type="text" name="name" class="input" value="{{ $user['name'] ?? '' }}" required>
              </div>
              <div>
                <label style="font-size:.82rem;font-weight:600;display:block;margin-bottom:6px">Phone</label>
                <input type="tel" name="phone" class="input" value="{{ $user['phone'] ?? '' }}">
              </div>
            </div>
            <div style="margin-bottom:20px">
              <label style="font-size:.82rem;font-weight:600;display:block;margin-bottom:6px">Skin Type</label>
              <select name="skin_type" class="input">
                <option value="">Not specified</option>
                @foreach(['Oily','Dry','Combination','Sensitive','Normal'] as $type)
                  <option value="{{ $type }}" {{ ($user['skin_type'] ?? '') === $type ? 'selected' : '' }}>{{ $type }}</option>
                @endforeach
              </select>
            </div>
            <button type="submit" class="btn btn-dark">Save Changes</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>
@endsection
